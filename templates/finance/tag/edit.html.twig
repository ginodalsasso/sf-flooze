{% extends 'layout/app.html.twig' %}

{% block title %}Modifier « {{ tag.name }} » — Flooze{% endblock %}

{% block content %}
<twig:PageHeader title="Modifier le tag" subtitle="{{ tag.name }}" />

<div class="page-content">
    <div class="form-page">
        <twig:Flash levels="{{ ['error'] }}" />
        {% include 'finance/tag/forms/_form.html.twig' with {submit_label: 'Enregistrer'} %}
    </div>
</div>
{% endblock %}
