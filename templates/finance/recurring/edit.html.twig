{% extends 'layout/app.html.twig' %}

{% block title %}Modifier « {{ recurrence.label }} » — Flooze{% endblock %}

{% block content %}
<twig:PageHeader title="Modifier la récurrence" subtitle="{{ recurrence.label }}" />

<div class="page-content">
    <div class="form-page">
        <twig:Flash levels="{{ ['error'] }}" />
        {% include 'finance/recurring/forms/_form.html.twig' with {submit_label: 'Enregistrer'} %}
    </div>
</div>
{% endblock %}
