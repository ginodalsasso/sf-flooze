{% extends 'layout/app.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {% include 'finance/transaction/_styles.html.twig' %}
{% endblock %}

{% block title %}Modifier la transaction — Flooze{% endblock %}

{% block content %}
<twig:PageHeader
    title="Modifier la transaction"
    subtitle="{{ transaction.description ?? transaction.date|date('d/m/Y') }}"
/>

<div class="page-content">
    <div class="form-page">
        <twig:Flash levels="{{ ['error'] }}" />
        {% if transaction.account.type.value in ['crypto', 'stock'] %}
            {% include 'finance/transaction/forms/_asset_form.html.twig' with {submit_label: 'Enregistrer', redirect_to: redirect_to} %}
        {% else %}
            {% include 'finance/transaction/forms/_classic_form.html.twig' with {submit_label: 'Enregistrer', redirect_to: redirect_to} %}
        {% endif %}
    </div>
</div>
{% endblock %}
