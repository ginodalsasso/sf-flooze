<style>
.acc-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 16px;
}

.acc-card {
  background: var(--bg-surface);
  border: 1px solid var(--border-strong);
  border-radius: 14px;
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 16px;
  box-shadow: var(--shadow-card);
  position: relative;
  transition: border-color .15s, box-shadow .15s;
}

.acc-card:hover {
  border-color: var(--fg-3);
  box-shadow: var(--shadow-card), 0 0 0 3px rgba(200,213,194,.08);
}

.acc-card-link {
  text-decoration: none;
  color: inherit;
}

.acc-card-link::after {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: 14px;
}

.acc-card-footer {
  position: relative;
  z-index: 1;
}

.acc-card-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
}

.acc-card-icon {
  width: 36px;
  height: 36px;
  border-radius: 10px;
  background: var(--bg-elevated);
  border: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.acc-card-icon i { width: 16px; height: 16px; color: var(--fg-2); }

.acc-card-name {
  font-family: 'Syne', sans-serif;
  font-size: 16px;
  font-weight: 600;
  color: var(--fg-1);
  margin: 0;
  line-height: 1.3;
}

.acc-card-balance {
  font-family: 'JetBrains Mono', monospace;
  font-size: 28px;
  font-weight: 600;
  color: var(--fg-1);
  letter-spacing: -0.02em;
}

.acc-card-balance--positive { color: #A8C4A0; }
.acc-card-balance--negative { color: #C2A89A; }

.acc-card-currency {
  font-family: 'DM Sans', sans-serif;
  font-size: 12px;
  color: var(--fg-3);
  margin-top: 2px;
}

.acc-card-footer {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 8px;
  padding-top: 12px;
  border-top: 1px solid var(--border);
}

.acc-card-footer form { margin: 0; }

/* ── Form ──────────────────────────────────────────────── */
.acc-form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
}

/* ── Show ──────────────────────────────────────────────── */
.acc-show-summary {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 20px;
  background: var(--bg-surface);
  border: 1px solid var(--border-strong);
  border-radius: 14px;
  padding: 20px 24px;
  box-shadow: var(--shadow-card);
  margin-bottom: 28px;
}

.acc-show-meta {
  display: flex;
  align-items: center;
  gap: 14px;
}

.acc-show-balance { text-align: center; }

.acc-show-actions {
  display: flex;
  align-items: center;
  gap: 8px;
}

.acc-show-section-title {
  font-family: 'Syne', sans-serif;
  font-size: 15px;
  font-weight: 600;
  color: var(--fg-2);
  margin: 0 0 12px;
  text-transform: uppercase;
  letter-spacing: .06em;
}
</style>
