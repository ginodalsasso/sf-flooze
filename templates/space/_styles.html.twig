<style>
/* ── Space type selector (page /spaces/new) ──────────────────── */
.space-type-selector {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}

.type-card {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding: 20px;
  background: var(--bg-elevated);
  border: 2px solid var(--border-strong);
  border-radius: 14px;
  cursor: pointer;
  transition: border-color .15s, background .15s, box-shadow .15s;
  user-select: none;
}

.type-card:hover:not(.type-card--disabled) {
  border-color: var(--fg-3);
  background: var(--bg-overlay);
}

.type-card:has(input[type="radio"]:checked) {
  border-color: var(--sage-400) !important;
  background: var(--bg-overlay) !important;
  box-shadow: var(--shadow-focus);
}

.type-card:has(input[type="radio"]:checked)::after {
  content: '';
  position: absolute;
  top: 14px;
  right: 14px;
  width: 8px;
  height: 8px;
  border-radius: 9999px;
  background: var(--sage-400);
}

.type-card--disabled {
  opacity: .45;
  cursor: not-allowed;
}

.type-card-icon {
  width: 40px;
  height: 40px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.type-card-icon i { width: 18px; height: 18px; }

.type-card-icon--personal      { background: rgba(154,175,194,.15); color: var(--color-info); }
.type-card-icon--professional  { background: rgba(168,196,160,.15); color: var(--color-positive); }

.type-card-title {
  font-family: 'Syne', sans-serif;
  font-size: 15px;
  font-weight: 600;
  color: var(--fg-1);
  margin-bottom: 4px;
}

.type-card-desc {
  font-size: 12px;
  color: var(--fg-2);
  line-height: 1.55;
}

.type-card-taken-badge {
  display: inline-flex;
  align-items: center;
  padding: 2px 8px;
  border-radius: 9999px;
  font-size: 10px;
  font-weight: 500;
  background: var(--bg-overlay);
  color: var(--fg-3);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  align-self: flex-start;
}

/* ── Type card readonly (page /spaces/{id}/edit) ─────────────── */
.type-card-readonly {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 16px;
  background: var(--bg-elevated);
  border: 1px solid var(--border-strong);
  border-radius: 12px;
  opacity: .6;
}

/* ── Spaces management list (page /spaces) ───────────────────── */
.spaces-list-full {
  display: flex;
  flex-direction: column;
  gap: 12px;
  max-width: 680px;
}

.space-full-card {
  background: var(--bg-surface);
  border: 1px solid var(--border-strong);
  border-radius: 14px;
  padding: 20px 24px;
  box-shadow: var(--shadow-card);
  display: flex;
  align-items: center;
  gap: 16px;
}

.space-full-icon {
  width: 44px;
  height: 44px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.space-full-icon i { width: 20px; height: 20px; }

.space-full-icon--personal      { background: rgba(154,175,194,.15); color: var(--color-info); }
.space-full-icon--professional  { background: rgba(168,196,160,.15); color: var(--color-positive); }

.space-full-body { flex: 1; min-width: 0; }

.space-full-name {
  font-family: 'Syne', sans-serif;
  font-size: 16px;
  font-weight: 600;
  color: var(--fg-1);
  margin-bottom: 3px;
}

.space-full-meta {
  font-size: 12px;
  color: var(--fg-3);
  display: flex;
  align-items: center;
  gap: 8px;
}

.space-full-meta .space-dot { display: inline-block; }

.space-full-actions {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-shrink: 0;
}

.space-full-actions form { margin: 0; }
</style>
